version: '2'

services:
  mongo:
    image: mongo
    restart: always
    # environment:
    #   - MONGO_INITDB_ROOT_USERNAME=root
    #   - MONGO_INITDB_ROOT_PASSWORD=password

    ports:
      - 27017:27017
    expose:
      - 27017
